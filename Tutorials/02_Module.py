# time is a module used here--this is prewritten code by some one else and we can call it by using import keyword
import time
# time Module has a sleep function 
# sleep function delays the execution of the program by 5 seconds
time.sleep(5)
print("5s paased since this program ran")
